node {
    checkout scm

    docker.withRegistry('https://hub.docker.com/', 'docker-cred') {

        def customImage = docker.build("newImage:${env.BUILD_ID}")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}